<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand ps-4" href="{{ path('main') }}">Main</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav">
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item pe-4">
                    <a class="nav-link" href="{{ path('admin') }}">Admin</a>
                </li>
            {% endif %}
        </ul>
        <ul class="navbar-nav ms-auto">
            {% if app.user %}
                <span class="navbar-text ms-auto pe-4">Hello, {{ app.user.name }}</span>
                <li class="nav-item pe-4">
                    <a class="nav-link" href="{{ path('logout') }}">Logout</a>
                </li>
            {% else %}
                <li class="nav-item pe-3">
                    <a class="btn btn-primary" href="{{ path('login') }}">Sing in</a>
                </li>
                <li class="nav-item pe-4">
                    <a class="btn btn-secondary" href="{{ path('register') }}">Sing up</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>